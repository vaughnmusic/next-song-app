import React from 'react'
import './ActiveRequests.css'

export default function ActiveRequests() {
    return (

        // page will render saved 'active requests' from db
        <div>ActiveRequests</div>
    )
}
